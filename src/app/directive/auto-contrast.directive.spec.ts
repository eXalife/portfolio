import { AutoContrastDirective } from './auto-contrast.directive';

describe('AutoContrastDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoContrastDirective();
    expect(directive).toBeTruthy();
  });
});
